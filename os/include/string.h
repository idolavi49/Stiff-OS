#ifndef	STRING_H
#define	STRING_H

#include "types.h"
uint16 strlength(string ch); // return string length


uint8 strEql(string ch1, string ch2); // This function compares two strings and returns true (1) if they are equal or false (0) if they are not equal


#endif